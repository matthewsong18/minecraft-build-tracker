export default class Coordinate {
  x: number;
  y: number;
  z: number;

  constructor(x_coord: number, y_coord: number, z_coord: number) {
    this.x = x_coord;
    this.y = y_coord;
    this.z = z_coord;
  }
}
